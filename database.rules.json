{
  "rules": {
    "coin_rush": {
      "$day": {
        ".read": "auth != null",

        "players": {
          "$uid": {
            ".write": "auth != null && auth.uid == $uid && newData.isString()"
          }
        },

        "claims": {
          "$coinId": {
            ".write": "auth != null && !data.exists() && newData.val() == auth.uid"
          }
        }
      }
    }
  }
}
