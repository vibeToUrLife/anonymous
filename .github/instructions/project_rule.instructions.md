---
description: everytime do the modification please also consider the firestore rules and write down in the firestore.rules file. Next, the code if can categorize then separate the file to the respective folder to avoid a file with too much code. Also need make the code maintainable and readable, so please add comments for the code that is not straightforward. NO hardcoded.do not help me direct commit the file. Every time I give you the instruction, you also need to consider the firestore read and write optimize to avoid over read and write.
# applyTo: 'Describe when these instructions should be loaded by the agent based on task context' # when provided, instructions will automatically be added to the request context when the pattern matches an attached file
---

<!-- Tip: Use /create-instructions in chat to generate content with agent assistance -->

Provide project context and coding guidelines that AI should follow when generating code, answering questions, or reviewing changes.